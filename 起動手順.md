# MoneyBoard User - 起動手順

## 🚀 起動コマンド（これだけ覚えればOK）

### 1回目（初回セットアップ）
```bash
# 依存関係インストール
npm install

# Vue.jsビルド
NODE_OPTIONS=--openssl-legacy-provider npm run dev

# サーバー起動
npx serve public -p 3000
```

### 2回目以降（普段の開発）
```bash
# サーバー起動するだけ
npx serve public -p 3000
```

**ブラウザで開く**: http://localhost:3000

---

## 📝 コードを編集したら

Vue.jsファイル（`resources/js/`内）を編集した場合：

```bash
# 再ビルド
NODE_OPTIONS=--openssl-legacy-provider npm run dev

# サーバーは起動したまま、ブラウザをリロード
```

### 自動ビルド（ファイル監視）したい場合

```bash
# ファイル変更を監視して自動ビルド
NODE_OPTIONS=--openssl-legacy-provider npm run watch
```

別のターミナルで：
```bash
# サーバー起動
npx serve public -p 3000
```

---

## 🎯 このプロジェクトの構造

- **フロントエンド（Vue.js）**: ローカルで開発・確認
- **バックエンド（API）**: VPSサーバー（http://mb-kdev-api.amb-dev.com/）を参照

→ バックエンドは触らなくてOK！フロントだけ開発できる

---

## 📂 よく編集するファイル

- `resources/js/views/` - ページコンポーネント
- `resources/js/components/` - 共通部品
- `resources/js/routes.js` - ルート設定
- `resources/sass/` - スタイル

---

## ⚠️ 注意点

- Node.js v22は新しすぎるので `NODE_OPTIONS=--openssl-legacy-provider` が必須
- ビルドすると `public/js/app.js` が更新される
- APIの向き先は `.env` の `MIX_VUE_APP_API_URL` で設定
- **`public/index.html` はローカル開発専用** → サーバーにアップしない（.gitignoreに追加済み）
