{
  "framework": null,
  "installCommand": "npm install",
  "buildCommand": "NODE_OPTIONS=--openssl-legacy-provider npx mix --production && rm -f public/hot public/index.php",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
